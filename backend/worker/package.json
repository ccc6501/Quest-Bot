{
  "name": "quest-handler",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler d1 migrations apply questing --remote && wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply questing --remote",
    "db:migrate:local": "wrangler d1 migrations apply questing --local"
  }
}
