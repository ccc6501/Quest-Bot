name = "quest-handler"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# Route under makerapp.cc/api/*
# Uncomment and set your zone_id + route after first deploy:
# routes = [{ pattern = "makerapp.cc/api/*", zone_id = "YOUR_ZONE_ID" }]

[[d1_databases]]
binding = "DB"
database_name = "questing"
# Created by deploy.sh — replaced automatically
database_id = "PLACEHOLDER"

[vars]
AI_PROVIDER = "openrouter"
OPENROUTER_MODEL = "anthropic/claude-3.5-sonnet"
# OPENROUTER_API_KEY is set via `wrangler secret put` (see deploy.sh)
